<% if object.respond_to?(:constructor) && object.constructor %>
  <%= layout_section('Constructor') %>
    <p><%= auto_link_code(object.constructor, :name => :long) %></p>
  <%= end_layout_section %>
  <%= include "leaf", :object => object.constructor %>
<% end %>
